# Live feed + violence detection + SMS deployment

cd /Users/sonamdobriyal/Test/live_feed

# 1) Configure
cp .env.example .env
# Edit: CAMERA_*, SMS_TO, TWILIO_*

pip3 install -r requirements.txt

# 2) Test SMS
python3 violence_monitor.py --test-sms

# 3) Run live (headless when DEPLOY_HEADLESS=true)
python3 violence_monitor.py

# Docker deploy
docker compose up -d --build
docker compose logs -f

# See DEPLOY.md for full VPS/systemd setup
