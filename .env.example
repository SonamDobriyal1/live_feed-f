# This repo uses two env files, not one:
#   pi/.env   — copy from pi/.env.example
#   web/.env  — copy from web/.env.example
#
# Shared values that must match: PORTAL_INGEST_KEY, SUPABASE_URL,
# SUPABASE_SERVICE_ROLE_KEY, SUPABASE_STORAGE_BUCKET
