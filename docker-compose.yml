services:
  violence-monitor:
    build: .
    container_name: violence-monitor
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./alerts:/app/alerts
    # Required if camera is on host LAN (Linux). On Mac/Windows use host network or VPN.
    # network_mode: host
